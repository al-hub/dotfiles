# TUI Sidebar Controlled Baseline

Generated: 2026-07-17T22:48:20+09:00

Revision: `v0.6.7` (promoted)

Environment: `tmux 3.6`, attached urxvt client, 100x30 geometry
Runs: 3

| Metric | Median and observed range | Result |
| :--- | :--- | :---: |
| Idle launcher CPU | 1.92 % (range 1.66-2.22) | PASS |
| Idle launcher peak RSS | 5788.00 KiB (range 5464.00-6236.00) | PASS |
| Active launcher CPU | 1.65 % (range 1.15-2.25) | PASS |
| Active launcher peak RSS | 5788.00 KiB (range 5464.00-6236.00) | PASS |
| Key-to-render latency | 48.00 ms (range 45.00-60.00) | FAIL |
| Enter-to-client-switch latency | 245.00 ms (range 236.00-312.00) | PASS |
| Archive completion | 349.00 ms (range 327.00-350.00) | PASS |
| Archive metadata size | 18078.00 bytes (range 18078.00-18090.00) | PASS |
| Restore completion | 1341.00 ms (range 1255.00-1371.00) | PASS |
| Restore pane/window integrity | 100% required on every run | PASS |
| Layout preserved after 3 open/close cycles | 100% required on every run | PASS |
| Grid bounded and exactly one cursor | required on every run | PASS |

Diagnostic trace: one trace-enabled run measured launcher-internal selection render at 1062us, `capture-pane` call time at 20ms, and 44ms from render completion to observation. The trace-enabled end-to-end value was 68ms because diagnostic tracing changes the path; the standard 3-run value remains 48ms. The remaining latency is therefore outside the selection renderer and is attributed to the tmux/PTY plus `capture-pane` observation path.

Persistent raw-stream diagnostic: a long-lived `pipe-pane` reader measured 52ms. This removes repeated `capture-pane` process creation but still exceeds the 40ms target, indicating that the remaining delay includes tmux/PTY propagation rather than only the polling observer.

## Method

Each run creates a unique tmux socket, an attached urxvt client, and a temporary history directory. It executes the launcher from the checked-out repository, never the installed copy. CPU is interval CPU time from `/proc/PID/stat` (including reaped children), and RSS is peak launcher RSS sampled during the same interval. Key-to-render checks poll capture-pane at a 10ms interval; other timed operations have bounded completion checks. A timeout or invariant mismatch fails the suite instead of becoming a numeric baseline.

The former active-vs-isolated comparison was removed because it changed the user's live tmux server and compared uncontrolled workloads. Use this report for before/after measurements under the same geometry and run count.

## Performance targets

Targets are reported independently from functional invariants. v0.6.7 is promoted with a documented external-latency exception: idle <=3%, active <=5%, switch <=1200ms, archive <=350ms, and restore <=2200ms pass by median; launcher-internal selection render is about 1ms. External key latency is 48ms against the 40ms target and remains a tmux/PTY observer-path follow-up item.
