# TUI Sidebar Controlled Baseline

Generated: 2026-07-17T22:38:42+09:00

Revision: `v0.6.5` baseline plus working-tree v0.6.6 candidate (dirty: `true`)

Environment: `tmux 3.6`, attached urxvt client, 100x30 geometry
Runs: 3

| Metric | Median and observed range | Result |
| :--- | :--- | :---: |
| Idle launcher CPU | 1.65 % (range 1.41-1.67) | PASS |
| Idle launcher peak RSS | 4544.00 KiB (range 4544.00-5716.00) | PASS |
| Active launcher CPU | 2.20 % (range 1.94-2.20) | PASS |
| Active launcher peak RSS | 4544.00 KiB (range 4544.00-5716.00) | PASS |
| Key-to-render latency | 49.00 ms (range 45.00-53.00) | FAIL |
| Enter-to-client-switch latency | 234.00 ms (range 231.00-248.00) | PASS |
| Archive completion | 341.00 ms (range 300.00-358.00) | PASS |
| Archive metadata size | 18054.00 bytes (range 18042.00-18066.00) | PASS |
| Restore completion | 1405.00 ms (range 1401.00-2348.00) | PASS |
| Restore pane/window integrity | 100% required on every run | PASS |
| Layout preserved after 3 open/close cycles | 100% required on every run | PASS |
| Grid bounded and exactly one cursor | required on every run | PASS |

## Method

Each run creates a unique tmux socket, an attached urxvt client, and a temporary history directory. It executes the launcher from the checked-out repository, never the installed copy. CPU is interval CPU time from `/proc/PID/stat` (including reaped children), and RSS is peak launcher RSS sampled during the same interval. Key-to-render checks poll capture-pane at a 10ms interval; other timed operations have bounded completion checks. A timeout or invariant mismatch fails the suite instead of becoming a numeric baseline.

The former active-vs-isolated comparison was removed because it changed the user's live tmux server and compared uncontrolled workloads. Use this report for before/after measurements under the same geometry and run count.

The lifecycle profile keeps the deterministic sleep-pane split/kill/layout fixture and the regression suite additionally exercises a launcher-owned sidebar: it kills and recreates the actual launcher pane and verifies that no pane ID residue or work-layout drift remains.

## Performance targets

Targets are reported independently from functional invariants. v0.6.6 is not promoted: idle <=3%, active <=5%, switch <=1200ms, and archive <=350ms pass by median, but key latency is 49ms against the 40ms target. Restore median passes at 1405ms, while one run reached 2348ms and remains a variance follow-up item.
